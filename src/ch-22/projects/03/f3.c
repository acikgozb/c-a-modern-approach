#include <stdio.h>

int main(void) {
  printf("hello from f3.c!\n");
  printf("I'm not as complex as f2, but a bit more than f1, so my file size is "
         "probably in between those.\n"
         "Though now that I wrote this, I'm not so sure.\n");

  return 0;
}
